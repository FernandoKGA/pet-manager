<%= stylesheet_link_tag "vaccination", "data-turbo-track": "reload" %>
<div class="dashboard-container">
  <div class="vaccine-page-container">
    <div class="vaccine-header">
      <%= link_to "← Voltar para o Pet", pet_vaccinations_path(@pet), class: 'btn-back' %>
      <h1 class="vaccine-title">Adicionar Nova Vacina</h1>
      <p class="vaccine-subtitle">Registre uma nova vacina para <%= @pet.name %>.</p>
    </div>
    <div class="vaccine-form-section">
      <%= render 'form' %>
    </div>
  </div>
</div>