<%= stylesheet_link_tag "vaccinations", "data-turbo-track": "reload" %>

<div class="dashboard-container">
  <div class="vaccination-form-page">
    <div class="vaccination-form-card">
      <%= link_to "← Voltar para as vacinas", pet_vaccinations_path(@pet), class: "btn-back" %>
      <h1>Editar vacina</h1>
      <p>Atualize os detalhes do registro de vacinação.</p>

      <%= render "form" %>
    </div>
  </div>
</div>
