<div class="pets-edit-page">
  <h1 class="title">Editar Pet</h1>

  <%= form_with(model: @pet, local: true, html: { class: "pet-form", multipart: true }) do |f| %>
    <% if @pet.errors.any? %>
      <div class="error-messages">
        <h3><%= pluralize(@pet.errors.count, "erro") %> impediram o salvamento:</h3>
        <ul>
          <% @pet.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group">
      <%= f.label :name, "Nome" %>
      <%= f.text_field :name, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= f.label :birthdate, "Data de Nascimento" %>
      <%= f.date_field :birthdate, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= f.label :size, "Tamanho" %>
      <%= f.number_field :size, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= f.label :species, "Espécie" %>
      <%= f.text_field :species, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= f.label :breed, "Raça" %>
      <%= f.text_field :breed, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= f.label :gender, "Gênero" %>
      <%= f.text_field :gender, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= f.label :sinpatinhas_id, "ID Sinpatinhas" %>
      <%= f.text_field :sinpatinhas_id, class: "form-control" %>
    </div>

    <!-- Seção de upload de foto -->
    <div class="form-group">
      <%= f.label :photo_file, "Foto do pet" %>
      <%= f.file_field :photo_file, accept: 'image/*', id: 'pet_photo_file', class: 'form-control-file' %>
      <small class="form-text text-muted">Tipos permitidos: png, jpg, jpeg, gif. Máx: 5MB.</small>

      <% if @pet&.photo_attached? %>
        <div class="current-photo" style="margin-top:10px;">
          <p>Foto atual:</p>
          <%= image_tag @pet.photo_data_uri, alt: @pet.name, style: "max-width: 300px; max-height: 300px; display:block; margin-bottom:8px;" %>
        </div>
      <% end %>

      <div id="photo_preview" style="margin-top:10px;"></div>
    </div>

    <div class="form-actions">
      <%= f.submit "Atualizar", class: "btn" %>
      <%= link_to "Cancelar", user_path(current_user), class: "btn btn-secondary", data: { turbo: false } %>
    </div>
  <% end %>
</div>

<!-- JS para preview da foto -->
<script>
  document.addEventListener('DOMContentLoaded', function() {
    var input = document.getElementById('pet_photo_file');
    var preview = document.getElementById('photo_preview');

    if (!input || !preview) return;

    input.addEventListener('change', function(event) {
      var file = event.target.files[0];
      if (!file) return;

      var allowed = ['image/png','image/jpg','image/jpeg','image/gif'];
      if (!allowed.includes(file.type)) {
        alert('Tipo de arquivo não permitido. Use png, jpg, jpeg ou gif.');
        input.value = '';
        return;
      }

      var maxBytes = 5 * 1024 * 1024;
      if (file.size > maxBytes) {
        alert('Arquivo muito grande. Máx 5MB.');
        input.value = '';
        return;
      }

      var reader = new FileReader();
      reader.onload = function(e) {
        preview.innerHTML = '<img src="' + e.target.result + '" style="max-width:300px; max-height:300px; display:block; margin-top:8px;" alt="Pré-visualização da foto" />';
      };
      reader.readAsDataURL(file);
    });
  });
</script>
